apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - configmap.yaml
  - mysql.yaml
  - redis.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml

configMapGenerator:
  - name: mysql-init
    namespace: flash-sale
    files:
      - init.sql=init.sql
